const options = {
  host: 'pecraftserver.aternos.me',
  port: '18256',
  username: 'bot'}
const bot = mineflayer.createBot(options)
bot.on('kicked', console.log)
bot.on('error', console.log)
bot.on('chat', (username, message) => {
    console.log(message)
})